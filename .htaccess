RewriteEngine On

# Se la richiesta punta già alla cartella pubblica, la lasciamo invariata.
RewriteRule ^public/ - [L]

# Tutto il traffico pubblico passa da /public.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/$1 [L]
